What does the player do, and what can they win?

- Game directory and run instructions:
- SDK version:
- RF cost, outcome table, and highest prize:
- Asset sources:
- Playable world, keyboard/touch movement and interactable activity locations:
- Verified the change stays in the current project and adds only the requested game component/runtime, without unrequested website navigation, footer or About/Store/catalog/detail pages:
- Publication metadata (only if requested; data for existing host UI):
- Verified all menus and controls stay inside the standard game container:
- Verified wallet connection and fresh hardwired-NFT ownership gate before simulated play:
- Checks run and known limitations:

Production publication requires separate Rare Friends review. A developer deployment does not publish a game.
